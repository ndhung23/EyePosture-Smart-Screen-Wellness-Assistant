{
  "name": "@eyeposture/api",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/server.js",
  "scripts": {
    "build": "npm --prefix ../.. run build -w @eyeposture/shared-types && npm --prefix ../.. run build -w @eyeposture/billing && tsc",
    "build:api": "npm --prefix ../.. run build -w @eyeposture/shared-types && npm --prefix ../.. run build -w @eyeposture/billing && tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@eyeposture/shared-types": "*",
    "@eyeposture/billing": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
