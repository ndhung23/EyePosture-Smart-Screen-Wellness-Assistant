{
  "name": "eyeposture-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "EyePosture - Commercial Desktop Screen Wellness Assistant",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @eyeposture/desktop",
    "desktop": "npm run start -w @eyeposture/desktop",
    "package:win": "npm run build -w @eyeposture/desktop && npx electron-packager apps/desktop EyePosture --platform=win32 --arch=x64 --out=release --overwrite --no-prune",
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron-packager": "^17.1.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
